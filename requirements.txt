# animalai>=3.1.3  # Optional: only needed for the "animalai" adapter_folder.
#                  # Requires Python 3.9.x or a compatible micro-version.
#                  # Default adapter_folder is "headless" (pure Python, no Unity needed).
faiss-cpu>=1.7.4
anthropic>=0.25.0
openai>=1.30.0
google-generativeai>=0.5.0
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
jsonlines>=4.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
coloredlogs>=15.0.2
